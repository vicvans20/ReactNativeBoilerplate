// Auth reducer?
// Probably handle the form data if I wanna use the reducer for such task
// Auth reducer?
// Probably handle the form data if I wanna use the reducer for such task
// import AC from '../constants';
const initialState = {
  currentUser: {},
  authToken: "",
  loading: false,
}

export default function sessionReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
